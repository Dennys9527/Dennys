# Snake Game

This repository contains a simple implementation of the classic snake game using [Pygame](https://www.pygame.org/). Run `python3 snake_game.py` to start the game.

If `pygame` is not installed, install it with `pip install pygame`.
